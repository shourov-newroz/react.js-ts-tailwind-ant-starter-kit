import { Button } from 'antd';

const App = () => (
  <div
    style={{
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
      height: '100vh',
    }}
  >
    <Button type='primary' size='large'>
      text
    </Button>
  </div>
);

export default App;
